# Скопируйте в backend/.env или задайте в панели Render (Environment / Secret File).
RESEND_API_KEY=
TO_EMAIL=
# После верификации домена в Resend — адрес на вашем домене; для теста: onboarding@resend.dev
FROM_EMAIL=onboarding@resend.dev
PORT=5000
